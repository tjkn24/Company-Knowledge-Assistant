# Prometheus configuration — tells Prometheus what to scrape
global:
  scrape_interval: 15s      # collect metrics every 15 seconds
  evaluation_interval: 15s

scrape_configs:
  - job_name: "agent"
    static_configs:
      - targets: ["agent:8000"]  # agent service name in Docker
    metrics_path: "/metrics"
